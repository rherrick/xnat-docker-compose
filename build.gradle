plugins {
  id "com.avast.gradle.docker-compose" version "0.6.17"
}

group "org.nrg.xnat.containers"
version "1.7.5-SNAPSHOT"

dockerCompose {
    appOnly {
        startedServices = [ 'xnat-web', 'xnat-db', 'prometheus', 'cadvisor']
        removeImages = "All"
    }
}

